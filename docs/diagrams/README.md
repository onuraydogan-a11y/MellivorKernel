# Diagrams

Supporting diagrams (architecture, sequence, data flow) for the kernel and
its subsystems. Prefer plain-text diagram sources (e.g. Mermaid) committed
alongside any rendered output, so diagrams stay diffable.

Empty for now.
