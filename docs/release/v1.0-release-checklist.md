# Mellivor Kernel v1.0 Release Checklist

Status: Not ready for `1.0.0`. Scope is now locked — see
[ADR-0019](../adr/0019-release-readiness-and-scope-lock.md).

This checklist records the release-readiness position of the current release
candidate (`v0.13.0`). It is an assessment of the responsibilities and
boundaries already defined by ADR-0002; it does not change the architecture
or schedule work by itself. `1.0.0` requires an explicit decision under
[ADR-0005](../adr/0005-versioning-strategy.md). As of Sprint 24,
[ADR-0019](../adr/0019-release-readiness-and-scope-lock.md) is that
decision's scope-defining precursor: it classifies every ADR-0002
responsibility as `Included in v1.0`, `Deferred to v1.1`, or `Future
research`, so this checklist's remaining open items are now closing
actions against a fixed target rather than open-ended questions. As of
Sprint 25, every `Included in v1.0` closing action is resolved and the
public API/compatibility policy is approved (the Public API Freeze
Audit). As of Sprint 26, the two remaining release-gate items (a
recorded gate run, and docs/examples parity) are also closed — see
[`docs/release/release-audit-sprint26.md`](release-audit-sprint26.md).
Declaring `1.0.0` itself remains a separate, later decision, per
ADR-0005 and ADR-0019: every "Release gate" item below is now checked,
but the declaration is a distinct, deliberate act this checklist does
not make by itself.

## Release gate

- [x] All kernel responsibilities in [ADR-0002](../adr/0002-ai-enterprise-kernel-scope-and-subsystems.md) have stable, documented contracts. *(Scope locked by ADR-0019; as of Sprint 25, every `Included in v1.0` closing action below is resolved.)*
- [x] The public API and compatibility policy are approved for the `1.0.0` milestone. *(Sprint 25 Public API Freeze Audit: all 18 kernel packages reviewed against ADR-0004; findings ratified in `docs/specs/memory.md`, `docs/specs/security.md`, `docs/specs/observability.md`, `docs/specs/plugins.md`, and `docs/specs/bootstrap.md`. No public API changed.)*
- [x] The release decision is recorded in an ADR or other approved release decision. *([ADR-0019](../adr/0019-release-readiness-and-scope-lock.md) — the scope-defining decision; the final "declare `1.0.0`" decision remains separate and still open.)*
- [x] The full test, lint, formatting, and strict type-checking gates pass for the release candidate. *(Sprint 26, 2026-07-28: see Verification Record below and [`docs/release/release-audit-sprint26.md`](release-audit-sprint26.md).)*
- [x] Documentation and examples describe the same supported behavior as the implementation. *(Sprint 26, 2026-07-28: parity confirmed, one drift item found and corrected — see [`docs/release/release-audit-sprint26.md`](release-audit-sprint26.md).)*

## Responsibility assessment

Classification per [ADR-0019](../adr/0019-release-readiness-and-scope-lock.md). "Closing action" is the specific, remaining step for each `Included in v1.0` item — everything else in a row is out of scope for `1.0.0` by design, not by omission.

| Responsibility | Classification | Closing action for v1.0 |
|---|---|---|
| AI orchestration | **Included in v1.0** — satisfied | None. Formally ratified stable by ADR-0019. |
| Agent lifecycle | Baseline **Included in v1.0** — satisfied; planning/reasoning/reflection/multi-agent **Future research** | None for the baseline (one agent, one workflow). Richer capability is explicitly not promised by `1.0.0`. |
| Workflow engine | Sequential execution **Included in v1.0** — satisfied; dynamic steps/parallel/scheduling **Deferred to v1.1** | None for sequential execution. Dynamic/parallel/scheduled execution is scheduled post-`1.0.0`, not before. |
| Memory abstraction | Text/in-memory **Included in v1.0** — satisfied; persistence **Deferred to v1.1**; embeddings/vector/RAG **Future research** | None for the current `InMemoryStore` contract. |
| Tool execution | **Included in v1.0** — satisfied | None. No known gap. |
| Event bus | In-process bus **Included in v1.0** — satisfied; distributed delivery **Future research** | None for `InMemoryEventBus`. |
| Plugin loading | Runtime + SDK + discovery + one built-in plugin **Included in v1.0** — satisfied; marketplace/remote plugins/sandboxing/hot reload/signature verification/package installation **Future research** | None for the current runtime/SDK/discovery scope. |
| Multi-LLM provider abstraction | `BaseProvider` + `ClaudeProvider` + `OpenAIProvider` **Included in v1.0** — satisfied; additional providers **Deferred to v1.1** | [x] Resolved in Sprint 25, no code change: exception granularity ratified as the required error-model shape; `ProviderCapabilities`'s current values ratified as accurate. See `docs/specs/providers.md`'s "v1.0 contract ratification" section. |
| Configuration | **Included in v1.0** — satisfied | None. No known gap. |
| Observability | Structured logging + contracts foundation + `execution` wiring **Included in v1.0** — satisfied as "bring your own backend"; concrete metrics/tracing vendor integration **Future research** | None. The `1.0.0` story is the pluggable contract, not a bundled vendor backend. |
| Security primitives | `authorization` + `security` contracts + audit wiring **Included in v1.0** — satisfied as "bring your own backend"; concrete `SecretProvider` backend **Deferred to v1.1**; authentication/OAuth/SSO/RBAC/encryption-at-rest **Future research** | None. The `1.0.0` story is permission-based authorization plus a pluggable audit/secrets contract, not a bundled identity platform. |

## Current limitations, by classification

Per [ADR-0019](../adr/0019-release-readiness-and-scope-lock.md). These are
recorded for release readiness only; resolving any `Deferred to v1.1` or
`Future research` item requires its own implementation, documentation,
tests, and architecture decisions where applicable — this checklist does
not authorize those changes.

**Deferred to v1.1** — real, scoped, schedulable directly once `1.0.0`
ships, no further design gate needed:

- Workflow: dynamic steps (a step's request built from an earlier step's
  result), parallel execution, scheduling.
- Memory: a second, persistent `MemoryStore` implementation.
- Providers: additional concrete providers (Gemini, local models).
- Security: a concrete `SecretProvider` backend.

**Future research** — open-ended, no committed design or timeline, in
several cases an open question of whether the capability belongs in the
kernel at all versus a consuming product:

- Agent lifecycle: planning, reasoning, reflection, multi-agent
  composition.
- Memory: embeddings, vector search, RAG.
- Event bus: distributed delivery (Kafka/NATS/Redis-backed).
- Plugin loading: marketplace, remote plugins, sandboxing, hot reload,
  signature verification, package installation.
- Observability: a concrete metrics/tracing vendor integration.
- Security: authentication, OAuth, SSO, RBAC, encryption-at-rest.

**Structural note, unaffected by this scope lock:**

- `bootstrap` itself still does not automatically compose the newer
  execution, authorization, workflow, and agent engines — but as of
  Sprint 22, `ai_engine.AIEngineBuilder` provides a concrete, supported
  composition mechanism on top of a bootstrapped `RuntimeContext`, so
  consumers no longer need to hand-wire that chain themselves.

## Verification record

Record the release-candidate commit, test results, lint and formatting results,
and strict type-checking results here when the gates are run.

- Candidate: `v0.13.0`
- Commit: `ffbb250fa6514c61bd5e74b7404cb89be5d63241`
- Verification date: 2026-07-28
- Reviewer: Sprint 26 automated verification pass (Claude Code CLI), run on behalf of onur.aydogan@mellivorsecurity.com — human sign-off on the Release Gate result is still pending
- Environment: Python 3.13.14; pytest 9.1.1; ruff 0.16.0; mypy 2.3.0 (compiled: yes)
- Tests (`pytest`): **717 passed** in 10.13s
- Ruff lint (`ruff check .`): **All checks passed!**
- Ruff format (`ruff format --check .`): **311 files already formatted**
- MyPy (`mypy`): **Success: no issues found in 246 source files**
- Review decision: All four gates green; no regressions. See [`docs/release/release-audit-sprint26.md`](release-audit-sprint26.md) for the full verification and documentation-parity record. Both remaining "Release gate" checklist items are now checked — the checklist's own release-gate bar is satisfied. The separate "declare `1.0.0`" decision under ADR-0005/ADR-0019 has not been made and is not made by this record.
